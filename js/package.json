{
  "private": true,
  "name": "@ramon/chat",
  "dependencies": {
    "flarum-tsconfig": "^1.0.3",
    "flarum-webpack-config": "^3.0.0",
    "typescript": "^5.5.4",
    "webpack": "^5.94.0",
    "webpack-cli": "^5.1.4"
  },
  "scripts": {
    "dev": "webpack --mode development --watch",
    "build": "webpack --mode production",
    "analyze": "cross-env ANALYZER=true yarn run build",
    "format": "prettier --write src",
    "build-typings": "tsc --declaration --declarationDir dist-typings --emitDeclarationOnly"
  },
  "devDependencies": {
    "emojibase-data": "^17.0.0",
    "simple-emoji-map": "^1.0.0"
  }
}
